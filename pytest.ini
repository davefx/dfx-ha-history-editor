[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Quiet noisy deprecation warnings from third-party libs (HA/SQLAlchemy) so
# failing tests are easier to spot in the output.
filterwarnings =
    ignore::DeprecationWarning
